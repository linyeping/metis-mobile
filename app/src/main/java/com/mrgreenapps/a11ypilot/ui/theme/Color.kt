package com.mrgreenapps.a11ypilot.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Claude Theme Colors - Warm, terracotta-inspired palette
 */
object ClaudeColors {
    // Light theme colors
    val Cream = Color(0xFFFAF9F5)
    val CreamCard = Color(0xFFF5F3ED)
    // Light-mode values stay unchanged. Dark mode uses the brighter variants below.
    val LightCoral = Color(0xFFCC785C)
    val Coral = Color(0xFFFF8A65)
    val CoralActive = Color(0xFFA9583E)
    val CoralDisabled = Color(0xFFE8D4CB)
    val LightAccentTeal = Color(0xFF3D9C9A)
    val AccentTeal = Color(0xFF4DB6AC)
    val DarkInk = Color(0xFF141413)
    val LightOnPrimary = Color(0xFFFAF9F5)
    val BodyText = Color(0xFF3D3D3A)
    val MutedText = Color(0xFF757570)
    val Border = Color(0xFFE0DED6)
    val SoftBorder = Color(0xFFEBE9E0)

    // Dark theme colors
    val DarkBg = Color(0xFF121212)
    val DarkSurface = Color(0xFF1E1E1E)
    val DarkElevated = Color(0xFF2C2C2C)
    val DarkSurfaceSoft = Color(0xFF383838)
    val OnDarkText = Color(0xFFE8E8E8)
    val OnDarkSoft = Color(0xFFB0B0B0)
    val OnDarkMuted = Color(0xFF808080)
    val DarkCoralActive = Color(0xFFFF7043)

    // Semantic colors
    val LightError = Color(0xFFD14343)
    val Error = Color(0xFFE57373)
}

/**
 * Codex Theme Colors - cool blue accents over a neutral Material dark surface.
 */
object CodexColors {
    // Light theme colors - kept unchanged
    val White = Color(0xFFFFFFFF)
    val LightBg = Color(0xFFF8F9FA)
    val LightSurface = Color(0xFFFFFFFF)
    val TechBlue = Color(0xFF168BE3)
    val TechBlueLight = Color(0xFF42B8F5)
    val AccentBlue = Color(0xFF29B6F6)
    val BodyText = Color(0xFF1A1A1A)
    val MutedText = Color(0xFF6B7280)
    val LightBorder = Color(0xFFE5E7EB)

    // Dark theme colors - neutral gray-black with softened blue accents
    val DarkBg = Color(0xFF121212)
    val DarkSurface = Color(0xFF1E1E1E)
    val DarkSurfaceElevated = Color(0xFF2C2C2C)
    val CyanAccent = Color(0xFF64B5F6)
    val CyanGlow = Color(0xFF1E88E5)
    val EmeraldAccent = Color(0xFF81C784)
    val OnDarkText = Color(0xFFE8E8E8)
    val OnDarkMuted = Color(0xFFB0B0B0)
    val DarkBorder = Color(0xFF2C2C2C)

    // Semantic colors - kept unchanged
    val Error = Color(0xFFEF4444)
}

<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is restricted to the MCP server's own LAN endpoint (localhost/private ranges) so a
  user-configured http:// API relay can no longer leak the API key in plaintext over the wire.
  Base config is explicit: cleartext disabled everywhere else.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.0.0</domain>
        <domain includeSubdomains="false">172.16.0.0</domain>
        <domain includeSubdomains="false">192.168.0.0</domain>
    </domain-config>
</network-security-config>
